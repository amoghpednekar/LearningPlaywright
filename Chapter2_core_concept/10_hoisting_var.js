console.log(a);
var a = "Amogh";
console.log(a);

// since var is function scoped, it will be hoisted to the top of the function
// and initialized with undefined   
// so the output will be undefined      