function amogh() {
    console.log(test);
    var test = "Amogh";
    console.log(test);

}
amogh();

// since function is function scoped, it will be hoisted to the top of the function
// and initialized with undefined   
// so the output will be undefined      
